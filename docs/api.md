<!-- Code generated by gomarkdoc. DO NOT EDIT -->

<!-- Generated-file formatting exceptions only:
MD001: gomarkdoc nests Go comment headings below declaration headings.
MD010: executable Go examples retain gofmt tabs inside code blocks.
MD012/MD022: gomarkdoc controls blank lines around anchors and examples.
MD024: examples repeat Output headings in separate symbol sections.
MD033: anchors and expandable examples use GitHub-supported HTML.
MD040: example output is emitted as an unlabelled literal code block.
-->
<!-- markdownlint-configure-file {"MD001": false, "MD010": {"code_blocks": false}, "MD012": false, "MD022": false, "MD024": {"siblings_only": true}, "MD033": {"allowed_elements": ["a", "details", "summary", "p"]}, "MD040": false} -->


# jev

```go
import "github.com/withzombies/jev-go"
```

Package jev is a small, unofficial client for TypeSafe's System One API. It uses only the Go standard library and returns typed probabilistic answers.

### Requests and answers

Construct a [Client](<#Client>) with [NewClient](<#NewClient>) and call [Client.SystemOne](<#Client.SystemOne>) with a [Request](<#Request>). A request combines JSON\-encodable state with named [Noul](<#Noul>), [Choice](<#Choice>), or [Score](<#Score>) questions. Each question is answered independently against the same state. A [NoulAnswer](<#NoulAnswer>) is a probability; a [ChoiceAnswer](<#ChoiceAnswer>) selects a label; a [ScoreAnswer](<#ScoreAnswer>) contains an expected score that may be fractional.

[RawQuestion](<#RawQuestion>) and Request.ExtraBody allow extensible payloads. Unknown answer types are retained as [RawAnswer](<#RawAnswer>). [Response.Nouls](<#Response.Nouls>), [Response.Choices](<#Response.Choices>), and [Response.Scores](<#Response.Scores>) provide typed answer maps. Token counts in [Usage](<#Usage>) are pointers: nil means unavailable, not zero. Use [Client.ListModels](<#Client.ListModels>) to discover available models through ModelsResponse.Models. [DefaultModel](<#DefaultBaseURL>) is a moving alias, not a pinned version.

### Configuration and ownership

The caller supplies credentials through [Config](<#Config>); this package never reads the environment. [ConfigFromEnv](<#ConfigFromEnv>) accepts an explicit lookup function and never creates a logger or enables retries. Logging requires an injected logger.

A nil Config.HTTPClient uses a ten\-second timeout. Inject a custom \*http.Client for transport and timeout control. The client is safe for concurrent calls, but callers must not mutate shared request data or the injected HTTP client while calls are in progress. Each method accepts a context and closes every response body. There is no pagination or input truncation.

Retries are opt\-in through Config.Retry or [WithRetry](<#WithRetry>); the zero policy makes one attempt. [DefaultRetryPolicy](<#DefaultRetryPolicy>) returns a configurable retry policy. [WithHeaders](<#WithHeaders>) and [WithTimeout](<#WithTimeout>) override headers and attempt timeout without mutating client defaults. A context deadline bounds the entire call, including retry waits. Per\-attempt timeouts include reading the response body.

[Client.SystemOneRaw](<#Client.SystemOneRaw>) and [Client.ListModelsRaw](<#Client.ListModelsRaw>) skip typed decoding and return [HTTPResponse](<#HTTPResponse>). Both typed responses also retain this fully buffered metadata. Network bodies are already closed, and HTTP metadata is excluded from typed response JSON serialization.

### Limits and errors

Config.MaxRequestBytes optionally limits the complete serialized request, including questions and JSON escaping. Zero disables that local limit. It measures bytes, not model tokens; a request passing it may still exceed the server's context limit. Applications decide how to select or reduce input.

Use errors.As to inspect [RequestSizeError](<#RequestSizeError>) for local size rejections and [APIError](<#APIError>) for non\-2xx responses. APIError.ErrorType may be "max\_tokens\_exceeded" for a server context rejection. APIError retains raw response data for diagnostics; its Body and Message may include submitted content. Its error string omits both. Endpoint and optional RetryAfter provide additional diagnostics without enabling retries.

[TransportError](<#TransportError>) preserves connection/body\-read causes for errors.Is and errors.As, including context cancellation, and exposes a Timeout method. Invalid or incomplete responses return [ResponseValidationError](<#ResponseValidationError>) with field paths and raw HTTP diagnostics.

See the [TypeSafe API documentation](<https://docs.typesafe.ai/api>) for the service contract and model limits.

## Index

- [Constants](<#constants>)
- [type APIError](<#APIError>)
  - [func \(e \*APIError\) Error\(\) string](<#APIError.Error>)
- [type Answer](<#Answer>)
- [type Choice](<#Choice>)
  - [func \(q Choice\) MarshalJSON\(\) \(\[\]byte, error\)](<#Choice.MarshalJSON>)
- [type ChoiceAnswer](<#ChoiceAnswer>)
  - [func \(a ChoiceAnswer\) MarshalJSON\(\) \(\[\]byte, error\)](<#ChoiceAnswer.MarshalJSON>)
- [type Client](<#Client>)
  - [func NewClient\(cfg Config\) \(\*Client, error\)](<#NewClient>)
  - [func \(c \*Client\) ListModels\(ctx context.Context, options ...RequestOption\) \(\*ModelsResponse, error\)](<#Client.ListModels>)
  - [func \(c \*Client\) ListModelsRaw\(ctx context.Context, options ...RequestOption\) \(\*HTTPResponse, error\)](<#Client.ListModelsRaw>)
  - [func \(c \*Client\) SystemOne\(ctx context.Context, request Request, options ...RequestOption\) \(\*Response, error\)](<#Client.SystemOne>)
  - [func \(c \*Client\) SystemOneRaw\(ctx context.Context, request Request, options ...RequestOption\) \(\*HTTPResponse, error\)](<#Client.SystemOneRaw>)
- [type Config](<#Config>)
  - [func ConfigFromEnv\(getenv func\(string\) string\) \(Config, error\)](<#ConfigFromEnv>)
- [type HTTPResponse](<#HTTPResponse>)
- [type Model](<#Model>)
- [type ModelsResponse](<#ModelsResponse>)
- [type Noul](<#Noul>)
  - [func \(q Noul\) MarshalJSON\(\) \(\[\]byte, error\)](<#Noul.MarshalJSON>)
- [type NoulAnswer](<#NoulAnswer>)
  - [func \(a NoulAnswer\) MarshalJSON\(\) \(\[\]byte, error\)](<#NoulAnswer.MarshalJSON>)
- [type NoulCriteria](<#NoulCriteria>)
- [type Question](<#Question>)
- [type RawAnswer](<#RawAnswer>)
  - [func \(a RawAnswer\) MarshalJSON\(\) \(\[\]byte, error\)](<#RawAnswer.MarshalJSON>)
- [type RawQuestion](<#RawQuestion>)
  - [func \(q RawQuestion\) MarshalJSON\(\) \(\[\]byte, error\)](<#RawQuestion.MarshalJSON>)
- [type Request](<#Request>)
  - [func \(r Request\) MarshalJSON\(\) \(\[\]byte, error\)](<#Request.MarshalJSON>)
- [type RequestOption](<#RequestOption>)
  - [func WithHeaders\(headers http.Header\) RequestOption](<#WithHeaders>)
  - [func WithRetry\(policy RetryPolicy\) RequestOption](<#WithRetry>)
  - [func WithTimeout\(timeout time.Duration\) RequestOption](<#WithTimeout>)
- [type RequestSizeError](<#RequestSizeError>)
  - [func \(e \*RequestSizeError\) Error\(\) string](<#RequestSizeError.Error>)
- [type Response](<#Response>)
  - [func \(r Response\) Choices\(\) map\[string\]ChoiceAnswer](<#Response.Choices>)
  - [func \(r Response\) Nouls\(\) map\[string\]NoulAnswer](<#Response.Nouls>)
  - [func \(r Response\) Scores\(\) map\[string\]ScoreAnswer](<#Response.Scores>)
  - [func \(r \*Response\) UnmarshalJSON\(data \[\]byte\) error](<#Response.UnmarshalJSON>)
- [type ResponseValidationError](<#ResponseValidationError>)
  - [func \(e \*ResponseValidationError\) Error\(\) string](<#ResponseValidationError.Error>)
  - [func \(e \*ResponseValidationError\) Unwrap\(\) error](<#ResponseValidationError.Unwrap>)
- [type RetryPolicy](<#RetryPolicy>)
  - [func DefaultRetryPolicy\(\) RetryPolicy](<#DefaultRetryPolicy>)
- [type Score](<#Score>)
  - [func \(q Score\) MarshalJSON\(\) \(\[\]byte, error\)](<#Score.MarshalJSON>)
- [type ScoreAnswer](<#ScoreAnswer>)
  - [func \(a ScoreAnswer\) MarshalJSON\(\) \(\[\]byte, error\)](<#ScoreAnswer.MarshalJSON>)
- [type TransportError](<#TransportError>)
  - [func \(e \*TransportError\) Error\(\) string](<#TransportError.Error>)
  - [func \(e \*TransportError\) Timeout\(\) bool](<#TransportError.Timeout>)
  - [func \(e \*TransportError\) Unwrap\(\) error](<#TransportError.Unwrap>)
- [type Usage](<#Usage>)


## Constants

<a name="DefaultBaseURL"></a>

```go
const (
    // DefaultBaseURL is the public TypeSafe API endpoint.
    DefaultBaseURL = "https://api.typesafe.ai"
    // DefaultModel follows TypeSafe's current stable Jev alias.
    DefaultModel = "jev-latest"
)
```

<a name="APIError"></a>
## type [APIError](<https://github.com/withzombies/jev-go/blob/main/errors.go#L15-L35>)

APIError is a non\-2xx response. Body and Headers retain server diagnostics, including retry hints. Error omits the body because it may echo input content.

```go
type APIError struct {
    // Message is the best-effort server explanation. It may echo request data and
    // is deliberately excluded from Error(). Empty means no recognized message.
    Message string
    // Endpoint is the HTTP method and relative API path.
    Endpoint string
    // RetryAfter is the parsed server delay at response receipt. Nil means absent
    // or invalid; zero is a valid immediate retry hint. It does not enable retries.
    RetryAfter *time.Duration
    // ErrorType is the best-effort detail.error_type value, or empty if unavailable.
    // The service uses "max_tokens_exceeded" for a context-limit rejection.
    ErrorType string
    // StatusCode is the HTTP response status.
    StatusCode int
    // Body holds the original response bytes and may contain submitted content.
    Body []byte
    // Headers is a cloned response header map, including any retry hints.
    Headers http.Header
    // RequestID is the x-typesafe-request-id response header, if present.
    RequestID string
}
```

<details><summary>Example</summary>
<p>



```go
package main

import (
	"context"
	"errors"
	"fmt"
	"net/http"
	"net/http/httptest"

	jev "github.com/withzombies/jev-go"
)

func main() {
	server := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, _ *http.Request) {
		w.Header().Set("x-typesafe-request-id", "req-example")
		w.WriteHeader(http.StatusBadRequest)
		if _, err := fmt.Fprint(w, `{"detail":{"error_type":"max_tokens_exceeded"}}`); err != nil {
			panic(err)
		}
	}))
	defer server.Close()
	client, err := jev.NewClient(jev.Config{APIKey: "example-key", BaseURL: server.URL, HTTPClient: server.Client()})
	if err != nil {
		panic(err)
	}
	_, err = client.SystemOne(context.Background(), jev.Request{
		State:     "sample",
		Questions: map[string]jev.Question{"ok": jev.Noul{Instructions: "Is the state valid?"}},
	})
	var apiErr *jev.APIError
	if errors.As(err, &apiErr) {
		// Inspect metadata without logging a body that could echo submitted content.
		fmt.Println(apiErr.StatusCode, apiErr.ErrorType, apiErr.RequestID)
	} else {
		panic(fmt.Sprintf("expected API rejection, got %v", err))
	}
}
```

#### Output

```
400 max_tokens_exceeded req-example
```

</p>
</details>

<a name="APIError.Error"></a>
### func \(\*APIError\) [Error](<https://github.com/withzombies/jev-go/blob/main/errors.go#L38>)

```go
func (e *APIError) Error() string
```

Error describes the status and request ID without exposing the response body.

<a name="Answer"></a>
## type [Answer](<https://github.com/withzombies/jev-go/blob/main/types.go#L98-L101>)

Answer is a [NoulAnswer](<#NoulAnswer>), [ChoiceAnswer](<#ChoiceAnswer>), [ScoreAnswer](<#ScoreAnswer>), or [RawAnswer](<#RawAnswer>), decoded by its wire type.

```go
type Answer interface {
    json.Marshaler
    // contains filtered or unexported methods
}
```

<a name="Choice"></a>
## type [Choice](<https://github.com/withzombies/jev-go/blob/main/types.go#L36-L41>)

Choice selects a label. A nil criterion leaves its label undescribed.

```go
type Choice struct {
    // Instructions describes the judgment to make against the request state.
    Instructions any `json:"instructions,omitempty"`
    // Criteria maps allowed labels to descriptions. A nil value leaves a label undescribed.
    Criteria map[string]any `json:"criteria"`
}
```

<a name="Choice.MarshalJSON"></a>
### func \(Choice\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/types.go#L65>)

```go
func (q Choice) MarshalJSON() ([]byte, error)
```

MarshalJSON encodes Choice with its API type discriminator.

<a name="ChoiceAnswer"></a>
## type [ChoiceAnswer](<https://github.com/withzombies/jev-go/blob/main/types.go#L110-L117>)

ChoiceAnswer contains the selected label and the complete distribution.

```go
type ChoiceAnswer struct {
    // Choice is the selected label.
    Choice string `json:"choice"`
    // Confidence is the service-provided confidence value, from zero to one.
    Confidence float64 `json:"confidence"`
    // Probabilities maps each allowed label to its probability.
    Probabilities map[string]float64 `json:"probabilities"`
}
```

<a name="ChoiceAnswer.MarshalJSON"></a>
### func \(ChoiceAnswer\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/types.go#L146>)

```go
func (a ChoiceAnswer) MarshalJSON() ([]byte, error)
```

MarshalJSON encodes ChoiceAnswer with its API type discriminator.

<a name="Client"></a>
## type [Client](<https://github.com/withzombies/jev-go/blob/main/client.go#L64-L76>)

Client can be reused concurrently. Callers must not mutate requests or the supplied HTTP client while calls are in progress. Retries require an explicit policy. The zero value is not usable; construct a Client with [NewClient](<#NewClient>).

```go
type Client struct {
    // contains filtered or unexported fields
}
```

<a name="NewClient"></a>
### func [NewClient](<https://github.com/withzombies/jev-go/blob/main/client.go#L81>)

```go
func NewClient(cfg Config) (*Client, error)
```

NewClient validates [Config](<#Config>) and supplies defaults for omitted settings. It performs no network requests. Invalid credentials, URLs, log levels, timeouts, byte limits, or retry policies return an error.

<details><summary>Example</summary>
<p>



```go
package main

import (
	"fmt"
	"net/http"
	"time"

	jev "github.com/withzombies/jev-go"
)

func main() {
	httpClient := &http.Client{Timeout: 2 * time.Second}
	client, err := jev.NewClient(jev.Config{
		APIKey:          "example-key",
		HTTPClient:      httpClient,
		MaxRequestBytes: 64 * 1024,
	})
	if err != nil {
		panic(err)
	}
	// Construction makes no network request and leaves the supplied client intact.
	fmt.Println("Ready:", client != nil)
	fmt.Println("Timeout:", httpClient.Timeout)
}
```

#### Output

```
Ready: true
Timeout: 2s
```

</p>
</details>

<a name="Client.ListModels"></a>
### func \(\*Client\) [ListModels](<https://github.com/withzombies/jev-go/blob/main/client.go#L164>)

```go
func (c *Client) ListModels(ctx context.Context, options ...RequestOption) (*ModelsResponse, error)
```

ListModels lists models and aliases with buffered HTTP metadata. Invalid model fields return \*ResponseValidationError. Options affect only this call.

<details><summary>Example</summary>
<p>



```go
package main

import (
	"context"
	"fmt"
	"net/http"
	"net/http/httptest"

	jev "github.com/withzombies/jev-go"
)

func main() {
	server := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, _ *http.Request) {
		if _, err := fmt.Fprint(w, `{"models":[{"name":"jev-example","description":"Example model","release_date":"2026-09-17"}]}`); err != nil {
			panic(err)
		}
	}))
	defer server.Close()
	client, err := jev.NewClient(jev.Config{APIKey: "example-key", BaseURL: server.URL, HTTPClient: server.Client()})
	if err != nil {
		panic(err)
	}
	models, err := client.ListModels(context.Background())
	if err != nil {
		panic(err)
	}
	for _, model := range models.Models {
		fmt.Println(model.Name, model.Description)
	}
}
```

#### Output

```
jev-example Example model
```

</p>
</details>

<a name="Client.ListModelsRaw"></a>
### func \(\*Client\) [ListModelsRaw](<https://github.com/withzombies/jev-go/blob/main/client.go#L203>)

```go
func (c *Client) ListModelsRaw(ctx context.Context, options ...RequestOption) (*HTTPResponse, error)
```

ListModelsRaw returns the fully buffered models response without decoding it. Non\-2xx responses still return \*APIError. It leaves no response body to close.

<a name="Client.SystemOne"></a>
### func \(\*Client\) [SystemOne](<https://github.com/withzombies/jev-go/blob/main/client.go#L122>)

```go
func (c *Client) SystemOne(ctx context.Context, request Request, options ...RequestOption) (*Response, error)
```

SystemOne answers each question independently against the same state. The effective payload after ExtraBody merging is validated, encoded once, and replayed unchanged on retries. Oversized requests fail before HTTP. Known answers are checked against the effective questions. Decode failures return \*ResponseValidationError with the original buffered HTTP response.

<details><summary>Example</summary>
<p>

Evaluate a question using an injected HTTP client and a caller\-owned deadline.

```go
package main

import (
	"context"
	"fmt"
	"net/http"
	"net/http/httptest"
	"time"

	jev "github.com/withzombies/jev-go"
)

func main() {
	// A local server keeps this executable example independent of credentials.
	server := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, _ *http.Request) {
		if _, err := fmt.Fprint(w, `{"model":"jev-test","usage":{"input_tokens":10,"output_tokens":2},"answers":{"billing":{"type":"noul","noul":0.98}}}`); err != nil {
			panic(err)
		}
	}))
	defer server.Close()
	client, err := jev.NewClient(jev.Config{APIKey: "example-key", BaseURL: server.URL, HTTPClient: server.Client()})
	if err != nil {
		panic(err)
	}
	ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
	defer cancel()
	response, err := client.SystemOne(ctx, jev.Request{
		State: "I was charged twice.",
		Questions: map[string]jev.Question{
			"billing": jev.Noul{Instructions: "Is this message about billing?"},
		},
	})
	if err != nil {
		panic(err)
	}
	answer := response.Nouls()["billing"]
	fmt.Printf("Probability of billing: %.2f\n", answer.Noul)
}
```

#### Output

```
Probability of billing: 0.98
```

</p>
</details>

<a name="Client.SystemOneRaw"></a>
### func \(\*Client\) [SystemOneRaw](<https://github.com/withzombies/jev-go/blob/main/client.go#L154>)

```go
func (c *Client) SystemOneRaw(ctx context.Context, request Request, options ...RequestOption) (*HTTPResponse, error)
```

SystemOneRaw evaluates a request and returns the fully buffered HTTP response without decoding answers. It applies the same request validation, byte cap, options and non\-2xx error handling as SystemOne, and closes the network body.

<details><summary>Example</summary>
<p>



```go
package main

import (
	"context"
	"fmt"
	"net/http"
	"net/http/httptest"
	"time"

	jev "github.com/withzombies/jev-go"
)

func main() {
	server := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, _ *http.Request) {
		w.Header().Set("x-typesafe-request-id", "example-request")
		if _, err := fmt.Fprint(w, `{"future_response":true}`); err != nil {
			panic(err)
		}
	}))
	defer server.Close()
	client, err := jev.NewClient(jev.Config{APIKey: "example-key", BaseURL: server.URL, HTTPClient: server.Client()})
	if err != nil {
		panic(err)
	}
	response, err := client.SystemOneRaw(context.Background(), jev.Request{State: "example", Questions: map[string]jev.Question{"ok": jev.Noul{Instructions: "Acceptable?"}}}, jev.WithTimeout(time.Second))
	if err != nil {
		panic(err)
	}
	// The body is buffered and the network resource has already been closed.
	fmt.Println(response.StatusCode, response.RequestID, string(response.Body))
}
```

#### Output

```
200 example-request {"future_response":true}
```

</p>
</details>

<a name="Config"></a>
## type [Config](<https://github.com/withzombies/jev-go/blob/main/client.go#L29-L59>)

Config explicitly configures a client; the library does not read environment variables. HTTPClient remains caller\-owned and is never modified or closed.

```go
type Config struct {
    // Retry is an opt-in policy. Its zero value disables retries.
    Retry RetryPolicy
    // LogBodies includes request and response contents at debug level. False
    // omits bodies even with a debug logger. Contents may contain sensitive data.
    LogBodies bool
    // DefaultModel is used when Request.Model is empty; empty selects DefaultModel.
    DefaultModel string
    // Headers supplies additional request headers; NewClient snapshots the map.
    Headers http.Header
    // Timeout overrides the HTTP client timeout per attempt. Zero inherits the
    // supplied client timeout, or ten seconds without a supplied client.
    Timeout time.Duration
    // Logger receives optional diagnostics. Nil disables all logging.
    Logger *slog.Logger
    // LogLevel filters diagnostics: debug, info, warn (default), error, or off.
    LogLevel string

    // APIKey is the required bearer credential. Surrounding whitespace is trimmed.
    APIKey string
    // BaseURL defaults to DefaultBaseURL. It must be an absolute HTTP(S) URL
    // without credentials, query parameters, or a fragment.
    BaseURL string
    // HTTPClient supplies the transport and timeout. Nil uses a new client with
    // a ten-second timeout. A supplied client is never modified or closed.
    HTTPClient *http.Client
    // MaxRequestBytes caps the complete encoded request, including JSON escaping,
    // questions, and model. Zero disables the cap; negative values are invalid.
    // This byte count is not a token count or a guarantee of server acceptance.
    MaxRequestBytes int
}
```

<a name="ConfigFromEnv"></a>
### func [ConfigFromEnv](<https://github.com/withzombies/jev-go/blob/main/config.go#L17>)

```go
func ConfigFromEnv(getenv func(string) string) (Config, error)
```

ConfigFromEnv loads optional configuration using the caller's lookup function, typically os.Getenv. It never accesses the process environment itself. Blank values are ignored. It reads TYPESAFE\_API\_KEY, TYPESAFE\_BASE\_URL, TYPESAFE\_DEFAULT\_MODEL and TYPESAFE\_LOG\_LEVEL. The caller may override fields before [NewClient](<#NewClient>); no logger is created and retries remain disabled. A nil lookup function or an invalid log level returns an error. Remaining configuration is validated by NewClient.

<details><summary>Example</summary>
<p>



```go
package main

import (
	"fmt"

	jev "github.com/withzombies/jev-go"
)

func main() {
	values := map[string]string{"TYPESAFE_API_KEY": "example-key", "TYPESAFE_DEFAULT_MODEL": "jev-latest"}
	cfg, err := jev.ConfigFromEnv(func(name string) string { return values[name] })
	if err != nil {
		panic(err)
	}
	// Applications can pass os.Getenv and then override individual fields.
	cfg.DefaultModel = "pinned-model"
	fmt.Println(cfg.DefaultModel, cfg.Logger == nil, cfg.Retry.MaxRetries)
}
```

#### Output

```
pinned-model true 0
```

</p>
</details>

<a name="HTTPResponse"></a>
## type [HTTPResponse](<https://github.com/withzombies/jev-go/blob/main/types.go#L198-L209>)

HTTPResponse is a fully buffered response. The network body is already closed; Headers and Body belong to this result. It is not a streaming interface.

```go
type HTTPResponse struct {
    // StatusCode is the HTTP response status.
    StatusCode int
    // Headers contains a snapshot of response headers.
    Headers http.Header
    // Body contains the original response bytes and may contain submitted data.
    Body []byte
    // RequestID is the x-typesafe-request-id header, or empty when absent.
    RequestID string
    // Endpoint is the HTTP method and relative API path.
    Endpoint string
}
```

<a name="Model"></a>
## type [Model](<https://github.com/withzombies/jev-go/blob/main/types.go#L187-L194>)

Model describes a model or alias accepted by the service.

```go
type Model struct {
    // Name is the model identifier accepted by Request.Model.
    Name string `json:"name"`
    // Description is the service-provided model description.
    Description string `json:"description"`
    // ReleaseDate preserves the service-provided release date string.
    ReleaseDate string `json:"release_date"`
}
```

<a name="ModelsResponse"></a>
## type [ModelsResponse](<https://github.com/withzombies/jev-go/blob/main/types.go#L212-L219>)

ModelsResponse holds available models and their originating HTTP response.

```go
type ModelsResponse struct {
    // Models lists the available models and aliases.
    Models []Model `json:"models"`
    // HTTPResponse contains buffered transport metadata, excluded from JSON.
    HTTPResponse *HTTPResponse `json:"-"`
    // RequestID is the x-typesafe-request-id header, if present.
    RequestID string `json:"-"`
}
```

<a name="Noul"></a>
## type [Noul](<https://github.com/withzombies/jev-go/blob/main/types.go#L20-L25>)

Noul asks for the probability that a statement is true.

```go
type Noul struct {
    // Instructions describes the judgment to make against the request state.
    Instructions any `json:"instructions,omitempty"`
    // Criteria optionally describes true and false outcomes; nil omits it.
    Criteria *NoulCriteria `json:"criteria,omitempty"`
}
```

<a name="Noul.MarshalJSON"></a>
### func \(Noul\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/types.go#L56>)

```go
func (q Noul) MarshalJSON() ([]byte, error)
```

MarshalJSON encodes Noul with its API type discriminator.

<a name="NoulAnswer"></a>
## type [NoulAnswer](<https://github.com/withzombies/jev-go/blob/main/types.go#L104-L107>)

NoulAnswer holds the probability of yes; it has no separate confidence field.

```go
type NoulAnswer struct {
    // Noul is the probability of true, from zero to one inclusive.
    Noul float64 `json:"noul"`
}
```

<a name="NoulAnswer.MarshalJSON"></a>
### func \(NoulAnswer\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/types.go#L137>)

```go
func (a NoulAnswer) MarshalJSON() ([]byte, error)
```

MarshalJSON encodes NoulAnswer with its API type discriminator.

<a name="NoulCriteria"></a>
## type [NoulCriteria](<https://github.com/withzombies/jev-go/blob/main/types.go#L28-L33>)

NoulCriteria optionally describes the two outcomes.

```go
type NoulCriteria struct {
    // True describes evidence for the statement; nil omits it.
    True any `json:"true,omitempty"`
    // False describes evidence against the statement; nil omits it.
    False any `json:"false,omitempty"`
}
```

<a name="Question"></a>
## type [Question](<https://github.com/withzombies/jev-go/blob/main/types.go#L14-L17>)

Question is one of [Noul](<#Noul>), [Choice](<#Choice>), [Score](<#Score>), or [RawQuestion](<#RawQuestion>). Instructions and descriptions may contain text, JSON objects, or arrays. See the API for supported content.

```go
type Question interface {
    json.Marshaler
    // contains filtered or unexported methods
}
```

<a name="RawAnswer"></a>
## type [RawAnswer](<https://github.com/withzombies/jev-go/blob/main/types.go#L222-L227>)

RawAnswer preserves a future answer type without interpreting its fields.

```go
type RawAnswer struct {
    // Type is the nonempty wire discriminator.
    Type string
    // Body is the complete JSON answer object, including its type.
    Body json.RawMessage
}
```

<a name="RawAnswer.MarshalJSON"></a>
### func \(RawAnswer\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/types.go#L232>)

```go
func (a RawAnswer) MarshalJSON() ([]byte, error)
```

MarshalJSON preserves the original JSON answer object.

<a name="RawQuestion"></a>
## type [RawQuestion](<https://github.com/withzombies/jev-go/blob/main/request.go#L11>)

RawQuestion forwards a JSON object, including extra fields and future types. A nonempty string "type" is required. Explicit nil values encode as JSON null. Known choice and score criteria receive the same structural checks as typed questions.

```go
type RawQuestion map[string]any
```

<details><summary>Example</summary>
<p>



```go
package main

import (
	"encoding/json"
	"fmt"

	jev "github.com/withzombies/jev-go"
)

func main() {
	question := jev.RawQuestion{"type": "noul", "instructions": nil, "future_option": true}
	encoded, err := json.Marshal(question)
	if err != nil {
		panic(err)
	}
	fmt.Println(string(encoded))
}
```

#### Output

```
{"future_option":true,"instructions":null,"type":"noul"}
```

</p>
</details>

<a name="RawQuestion.MarshalJSON"></a>
### func \(RawQuestion\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/request.go#L16>)

```go
func (q RawQuestion) MarshalJSON() ([]byte, error)
```

MarshalJSON encodes the complete question without dropping unknown fields.

<a name="Request"></a>
## type [Request](<https://github.com/withzombies/jev-go/blob/main/types.go#L84-L94>)

Request evaluates State against independently answered named Questions. State must encode as a JSON string, object, or array. Empty Model uses the client default.

```go
type Request struct {
    // ExtraBody shallow-merges over all standard fields, including explicit nulls.
    // Validation and the byte cap apply to the final encoded payload.
    ExtraBody map[string]any `json:"-"`
    // State is the shared JSON-encodable context for all questions.
    State any `json:"state"`
    // Questions maps caller-chosen identifiers to non-nil question values.
    Questions map[string]Question `json:"questions"`
    // Model selects a model name or alias; empty uses the client default.
    Model string `json:"model"`
}
```

<a name="Request.MarshalJSON"></a>
### func \(Request\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/request.go#L20>)

```go
func (r Request) MarshalJSON() ([]byte, error)
```

MarshalJSON shallow\-merges ExtraBody over the standard request fields. Objects are replaced, never deep\-merged. The caller's maps are not modified.

<a name="RequestOption"></a>
## type [RequestOption](<https://github.com/withzombies/jev-go/blob/main/config.go#L36>)

RequestOption adjusts one call without modifying the client. Options returned by this package may be reused concurrently. Later options take precedence.

```go
type RequestOption func(*requestOptions) error
```

<a name="WithHeaders"></a>
### func [WithHeaders](<https://github.com/withzombies/jev-go/blob/main/config.go#L46>)

```go
func WithHeaders(headers http.Header) RequestOption
```

WithHeaders merges a snapshot of headers over the client defaults. Header names are case insensitive. Authentication, protocol and SDK headers stay protected.

<a name="WithRetry"></a>
### func [WithRetry](<https://github.com/withzombies/jev-go/blob/main/retry.go#L57>)

```go
func WithRetry(policy RetryPolicy) RequestOption
```

WithRetry replaces the client's policy for one call with a snapshot of policy. Pass RetryPolicy\{\} to disable retries, or edit a [DefaultRetryPolicy](<#DefaultRetryPolicy>) copy to override individual SDK defaults without ambiguous zero values.

<a name="WithTimeout"></a>
### func [WithTimeout](<https://github.com/withzombies/jev-go/blob/main/config.go#L54>)

```go
func WithTimeout(timeout time.Duration) RequestOption
```

WithTimeout sets a positive timeout for each complete HTTP attempt, including response body delivery. It may override the injected HTTP client's timeout; the caller's context still bounds the entire operation.

<a name="RequestSizeError"></a>
## type [RequestSizeError](<https://github.com/withzombies/jev-go/blob/main/errors.go#L54-L59>)

RequestSizeError means the encoded request exceeded the caller's byte cap. It is a local size check, not a measurement of the model's token context.

```go
type RequestSizeError struct {
    // Size is the actual encoded request length in bytes.
    Size int
    // Limit is the configured maximum request length in bytes.
    Limit int
}
```

<details><summary>Example</summary>
<p>



```go
package main

import (
	"context"
	"errors"
	"fmt"

	jev "github.com/withzombies/jev-go"
)

func main() {
	client, err := jev.NewClient(jev.Config{APIKey: "example-key", MaxRequestBytes: 1})
	if err != nil {
		panic(err)
	}
	// This request is rejected locally; no HTTP request is made.
	_, err = client.SystemOne(context.Background(), jev.Request{
		State:     "sample",
		Questions: map[string]jev.Question{"ok": jev.Noul{Instructions: "Is the state valid?"}},
	})
	var sizeErr *jev.RequestSizeError
	if errors.As(err, &sizeErr) {
		fmt.Println("Over budget:", sizeErr.Size > sizeErr.Limit)
	} else {
		panic(fmt.Sprintf("expected size rejection, got %v", err))
	}
}
```

#### Output

```
Over budget: true
```

</p>
</details>

<a name="RequestSizeError.Error"></a>
### func \(\*RequestSizeError\) [Error](<https://github.com/withzombies/jev-go/blob/main/errors.go#L62>)

```go
func (e *RequestSizeError) Error() string
```

Error reports the measured byte count and configured limit.

<a name="Response"></a>
## type [Response](<https://github.com/withzombies/jev-go/blob/main/types.go#L172-L184>)

Response contains one typed answer per question and server metadata.

```go
type Response struct {
    // HTTPResponse holds buffered transport metadata; nil for manually decoded values.
    // It is excluded from JSON serialization and owns no open network resources.
    HTTPResponse *HTTPResponse `json:"-"`
    // Model is the resolved model used for evaluation.
    Model string `json:"model"`
    // Answers maps question identifiers to typed answer values.
    Answers map[string]Answer `json:"answers"`
    // Usage contains the token counts returned by the service.
    Usage Usage `json:"usage"`
    // RequestID is the x-typesafe-request-id response header, if present.
    RequestID string `json:"-"`
}
```

<a name="Response.Choices"></a>
### func \(Response\) [Choices](<https://github.com/withzombies/jev-go/blob/main/types.go#L239>)

```go
func (r Response) Choices() map[string]ChoiceAnswer
```

Choices returns a new map containing only typed choice answers. Nested maps within each answer remain shared with Answers.

<a name="Response.Nouls"></a>
### func \(Response\) [Nouls](<https://github.com/withzombies/jev-go/blob/main/types.go#L235>)

```go
func (r Response) Nouls() map[string]NoulAnswer
```

Nouls returns a new map containing only typed yes/no answers.

<details><summary>Example</summary>
<p>



```go
package main

import (
	"encoding/json"
	"fmt"

	jev "github.com/withzombies/jev-go"
)

func main() {
	var response jev.Response
	if err := json.Unmarshal([]byte(`{"model":"jev","usage":{},"answers":{"ok":{"type":"noul","noul":0.9}}}`), &response); err != nil {
		panic(err)
	}
	fmt.Println(response.Nouls()["ok"].Noul)
	fmt.Println(response.Usage.InputTokens == nil)
}
```

#### Output

```
0.9
true
```

</p>
</details>

<a name="Response.Scores"></a>
### func \(Response\) [Scores](<https://github.com/withzombies/jev-go/blob/main/types.go#L243>)

```go
func (r Response) Scores() map[string]ScoreAnswer
```

Scores returns a new map containing only typed score answers. Nested maps within each answer remain shared with Answers.

<a name="Response.UnmarshalJSON"></a>
### func \(\*Response\) [UnmarshalJSON](<https://github.com/withzombies/jev-go/blob/main/types.go#L257>)

```go
func (r *Response) UnmarshalJSON(data []byte) error
```

UnmarshalJSON decodes known answers strictly and preserves future answer types as RawAnswer. Missing required fields never become implicit zero\-valued answers. Usage counts may be absent or null; transport metadata is not read from JSON.

<a name="ResponseValidationError"></a>
## type [ResponseValidationError](<https://github.com/withzombies/jev-go/blob/main/errors.go#L89-L97>)

ResponseValidationError reports an invalid successful response. HTTPResponse preserves the original bytes for diagnostics; Error never includes the body.

```go
type ResponseValidationError struct {
    // FieldPath identifies the offending field, such as answers.name.confidence.
    // Empty denotes invalid top-level JSON. Dots separate object keys and indices.
    FieldPath string
    // HTTPResponse is the fully buffered response that failed validation.
    HTTPResponse *HTTPResponse
    // Err is the underlying JSON or structural validation error.
    Err error
}
```

<a name="ResponseValidationError.Error"></a>
### func \(\*ResponseValidationError\) [Error](<https://github.com/withzombies/jev-go/blob/main/errors.go#L100>)

```go
func (e *ResponseValidationError) Error() string
```

Error describes the invalid field without exposing server content.

<a name="ResponseValidationError.Unwrap"></a>
### func \(\*ResponseValidationError\) [Unwrap](<https://github.com/withzombies/jev-go/blob/main/errors.go#L109>)

```go
func (e *ResponseValidationError) Unwrap() error
```

Unwrap preserves the underlying decoding or validation cause.

<a name="RetryPolicy"></a>
## type [RetryPolicy](<https://github.com/withzombies/jev-go/blob/main/retry.go#L16-L40>)

RetryPolicy controls retries after a failed HTTP attempt. Its zero value makes one attempt. Policies are copied by [NewClient](<#NewClient>) and [WithRetry](<#WithRetry>), including statuses. AdditionalRetry must be safe for concurrent calls if the client is shared.

```go
type RetryPolicy struct {
    // MaxRetries is the maximum number of attempts after the initial attempt.
    MaxRetries int
    // BackoffInitial is the first exponential delay; zero disables backoff.
    BackoffInitial time.Duration
    // BackoffMax caps exponential delays; zero disables backoff.
    BackoffMax time.Duration
    // BackoffJitter is the randomly subtracted fraction of a delay, in [0,1].
    BackoffJitter float64
    // HTTPStatuses lists retryable HTTP statuses. Nil retries no HTTP statuses.
    HTTPStatuses []int
    // RespectRetryAfter enables retry-after-ms and Retry-After response hints.
    RespectRetryAfter bool
    // MaxRetryAfter bounds accepted server delays; zero allows any representable
    // delay. Longer hints fall back to exponential backoff.
    MaxRetryAfter time.Duration
    // RetryConnectionErrors includes connection failures and interrupted bodies.
    RetryConnectionErrors bool
    // RetryTimeouts controls attempt timeouts independently of connection errors.
    RetryTimeouts bool
    // AdditionalRetry may opt HTTP, transport or response-validation failures into retrying.
    // It cannot override caller cancellation or the retry limit. Local validation
    // and encoding errors never reach it.
    AdditionalRetry func(error) bool
}
```

<a name="DefaultRetryPolicy"></a>
### func [DefaultRetryPolicy](<https://github.com/withzombies/jev-go/blob/main/retry.go#L46>)

```go
func DefaultRetryPolicy() RetryPolicy
```

DefaultRetryPolicy returns a fresh SDK\-style policy: two retries, 500ms initial backoff capped at 5s, 25% jitter, HTTP 408/429/5xx, connection errors and attempt timeouts. Server delay hints up to 60s are honored. It is never enabled implicitly. Use a context deadline to bound the total call, including waits and all attempts.

<details><summary>Example</summary>
<p>



```go
package main

import (
	"fmt"

	jev "github.com/withzombies/jev-go"
)

func main() {
	policy := jev.DefaultRetryPolicy()
	policy.MaxRetries = 3
	policy.HTTPStatuses = []int{429, 503}
	fmt.Println(policy.MaxRetries, policy.BackoffInitial, policy.BackoffMax)
	fmt.Println(policy.HTTPStatuses)
	// Pass policy in Config.Retry or WithRetry(policy). A context deadline bounds
	// the whole call; WithRetry(jev.RetryPolicy{}) disables retries for one call.

}
```

#### Output

```
3 500ms 5s
[429 503]
```

</p>
</details>

<a name="Score"></a>
## type [Score](<https://github.com/withzombies/jev-go/blob/main/types.go#L44-L49>)

Score evaluates an ordered rubric whose levels start at zero.

```go
type Score struct {
    // Instructions describes the judgment to make against the request state.
    Instructions any `json:"instructions,omitempty"`
    // Criteria lists at least one level description, starting at score zero.
    Criteria []any `json:"criteria"`
}
```

<a name="Score.MarshalJSON"></a>
### func \(Score\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/types.go#L74>)

```go
func (q Score) MarshalJSON() ([]byte, error)
```

MarshalJSON encodes Score with its API type discriminator.

<a name="ScoreAnswer"></a>
## type [ScoreAnswer](<https://github.com/withzombies/jev-go/blob/main/types.go#L121-L130>)

ScoreAnswer contains an expected, possibly fractional score. Map keys are zero\-based rubric levels as strings, matching the API's JSON representation.

```go
type ScoreAnswer struct {
    // Score is the expected score and may lie between rubric levels.
    Score float64 `json:"score"`
    // Confidence is the service-provided confidence value, from zero to one.
    Confidence float64 `json:"confidence"`
    // Probabilities maps each zero-based rubric level, encoded as a string, to its probability.
    Probabilities map[string]float64 `json:"probabilities"`
    // Legend maps zero-based rubric levels, encoded as strings, to descriptions.
    Legend map[string]any `json:"legend"`
}
```

<a name="ScoreAnswer.MarshalJSON"></a>
### func \(ScoreAnswer\) [MarshalJSON](<https://github.com/withzombies/jev-go/blob/main/types.go#L155>)

```go
func (a ScoreAnswer) MarshalJSON() ([]byte, error)
```

MarshalJSON encodes ScoreAnswer with its API type discriminator.

<a name="TransportError"></a>
## type [TransportError](<https://github.com/withzombies/jev-go/blob/main/errors.go#L68-L73>)

TransportError reports a connection or response\-body delivery failure. Err is retained for errors.Is and errors.As, including context deadlines.

```go
type TransportError struct {
    // Endpoint contains the HTTP method and relative API path.
    Endpoint string
    // Err is the underlying network or body-read error.
    Err error
}
```

<a name="TransportError.Error"></a>
### func \(\*TransportError\) [Error](<https://github.com/withzombies/jev-go/blob/main/errors.go#L76>)

```go
func (e *TransportError) Error() string
```

Error describes the failed operation and underlying cause.

<a name="TransportError.Timeout"></a>
### func \(\*TransportError\) [Timeout](<https://github.com/withzombies/jev-go/blob/main/errors.go#L82>)

```go
func (e *TransportError) Timeout() bool
```

Timeout reports whether the cause implements net.Error with Timeout true.

<a name="TransportError.Unwrap"></a>
### func \(\*TransportError\) [Unwrap](<https://github.com/withzombies/jev-go/blob/main/errors.go#L79>)

```go
func (e *TransportError) Unwrap() error
```

Unwrap returns the underlying cause.

<a name="Usage"></a>
## type [Usage](<https://github.com/withzombies/jev-go/blob/main/types.go#L164-L169>)

Usage contains the token counts returned by the service.

```go
type Usage struct {
    // InputTokens is the reported input count; nil means absent or null.
    InputTokens *int `json:"input_tokens"`
    // OutputTokens is the reported output count; nil means absent or null.
    OutputTokens *int `json:"output_tokens"`
}
```

[Home](../README.md) · [Library guide](guide.md) · [Triage](triage.md) · [Development](development.md)

Generated from Go doc comments and tested examples. Edit the Go source, then run `make docs`.


Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
